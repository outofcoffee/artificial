# Secrets — contains the OpenRouter API key
.env

# Compiled binary (`go build -o outfit .`, or the default `go build .` name)
/outfit

# GoReleaser output
/dist/

# Coverage artefacts
coverage.out
coverage.html
